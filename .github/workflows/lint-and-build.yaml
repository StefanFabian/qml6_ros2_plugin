name: Build & Test
on:
  push:
    branches: [ humble ]
  pull_request:
    branches: [ humble ]
defaults:
  run:
    shell: bash
jobs:
  Linting:
    name: Lint Code Base
    runs-on: ubuntu-22.04
    steps:
      - name: Checkout Code
        uses: actions/checkout@v4
        with:
          fetch-depth: 0
      - name: Setup
        run: |
          sudo apt-get update && sudo apt-get install -y cppcheck libxml2-utils
      - name: Check C++
        run: |
          cppcheck --force --library=qt --quiet --error-exitcode=1 include src
      - name: Lint package.xml
        run: |
          xmllint --noout --schema http://download.ros.org/schema/package_format3.xsd $(find . -name 'package.xml')

  build-and-test:
    needs: Linting
    runs-on: ${{ matrix.setup.os }}
    container:
      image: ros:${{ matrix.setup.rosdistro }}-ros-base
    strategy:
      matrix:
        setup:
          - rosdistro: humble
            os: ubuntu-22.04
    steps:
      - name: install build tools and latest testing repo
        run: |
          sudo apt-get update
          sudo apt-get install -y ros-dev-tools ros2-testing-apt-source
          sudo apt-get update
      - uses: actions/checkout@v4
        with:
          path: src/qml6_ros2_plugin
      - name: rosdep
        run: |
          rosdep update --rosdistro ${{ matrix.setup.rosdistro }} --include-eol-distros
          rosdep install -y --from-paths src --ignore-src --rosdistro ${{ matrix.setup.rosdistro }}
      - name: build
        run: |
          source /opt/ros/${{ matrix.setup.rosdistro }}/setup.bash
          colcon build
      - name: test
        run: |
          source /opt/ros/${{ matrix.setup.rosdistro }}/setup.bash
          colcon test --packages-select qml6_ros2_plugin
          colcon test-result --verbose
